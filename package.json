{
  "name": "comps-saves-likes-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "health": "node -e \"fetch('http://localhost:' + (process.env.PORT || 4173) + '/health').then(r => { if (!r.ok) process.exit(1); console.log('ok'); })\""
  },
  "engines": {
    "node": ">=18"
  }
}
